---
// No imports needed.
---

<style>
  /* 1. DEFINE THE ANIMATIONS (Slow floating effect) */
  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }
  .animate-blob {
    animation: blob 7s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  .animation-delay-4000 {
    animation-delay: 4s;
  }
</style>

<section class="relative pt-32 pb-20 lg:pt-48 lg:pb-32 overflow-hidden bg-slate-50 border-b border-slate-200">
  
  <div class="absolute inset-0 z-0 opacity-[0.4]" style="background-image: linear-gradient(rgba(148, 163, 184, 0.2) 1px, transparent 1px), linear-gradient(90deg, rgba(148, 163, 184, 0.2) 1px, transparent 1px); background-size: 40px 40px;"></div>

  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full z-0 pointer-events-none">
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-blue-400 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
      <div class="absolute top-0 right-1/4 w-96 h-96 bg-cyan-300 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
      <div class="absolute -bottom-32 left-1/3 w-96 h-96 bg-purple-300 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-4000"></div>
  </div>

  <div class="absolute inset-0 z-0 opacity-40 mix-blend-overlay pointer-events-none" style="background-image: url('data:image/svg+xml,%3Csvg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cfilter id=%22noiseFilter%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.65%22 numOctaves=%223%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23noiseFilter)%22/%3E%3C/svg%3E');"></div>


  <div class="max-w-7xl mx-auto px-4 sm:px-6 text-center relative z-10">
    
    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/60 border border-slate-200 backdrop-blur-sm text-slate-600 text-xs font-bold mb-8 shadow-sm hover:scale-105 transition-transform cursor-default">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
      </span>
      Limited Slots Available: Taking New Clients
    </div>
    
    <h1 class="text-5xl md:text-7xl font-extrabold text-slate-900 tracking-tight mb-8 leading-tight">
      Get discovered by <br class="hidden md:block" />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-cyan-500 to-blue-600 animate-gradient">more customers.</span>
    </h1>

    <p class="text-xl text-slate-600 mb-10 max-w-2xl mx-auto leading-relaxed">
      Stop renting your traffic. We build <strong>High-Performance Signal Engines</strong> 
      that physically map your business to local search results.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      
      <a href="https://tally.so/r/aQ52Lv" target="_blank" class="group w-full sm:w-auto px-8 py-4 bg-blue-600 text-white font-bold rounded-xl hover:bg-blue-500 transition-all flex items-center justify-center gap-2 shadow-lg shadow-blue-500/40 hover:shadow-blue-500/60 hover:-translate-y-1">
        Start My Project
        <i data-lucide="arrow-right" class="w-5 h-5 group-hover:translate-x-1 transition-transform"></i>
      </a>
      
      <a href="#how-it-works" class="w-full sm:w-auto px-8 py-4 bg-white/50 backdrop-blur-sm text-slate-900 font-bold rounded-xl border border-slate-200 hover:bg-white transition-all flex items-center justify-center hover:shadow-lg hover:border-blue-200">
        How it works
      </a>
    </div>

    <div class="mt-16 pt-8 border-t border-slate-200/60 flex flex-col items-center gap-4">
        <p class="text-xs font-bold text-slate-400 uppercase tracking-widest">Scaling businesses in</p>
        
        <div class="flex flex-wrap justify-center gap-x-8 gap-y-4 opacity-60 grayscale hover:grayscale-0 transition-all duration-500">
            <span class="font-bold text-slate-900 text-lg flex items-center gap-2"><i data-lucide="map-pin" class="w-4 h-4 text-blue-500"></i> KL</span>
            <span class="font-bold text-slate-900 text-lg flex items-center gap-2"><i data-lucide="map-pin" class="w-4 h-4 text-blue-500"></i> Selangor</span>
            <span class="font-bold text-slate-900 text-lg flex items-center gap-2"><i data-lucide="map-pin" class="w-4 h-4 text-blue-500"></i> Penang</span>
            <span class="font-bold text-slate-900 text-lg flex items-center gap-2"><i data-lucide="map-pin" class="w-4 h-4 text-blue-500"></i> Johor</span>
        </div>
    </div>

  </div>
</section>